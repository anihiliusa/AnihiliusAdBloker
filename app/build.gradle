plugins {
    id 'com.android.application'
}

android {
    namespace 'com.anihiliusa.xtube'
    compileSdk 34

    defaultConfig {
        applicationId 'com.anihiliusa.xtube'
        minSdk 24
        targetSdk 34
        versionCode 132
        versionName '1.3.2'
    }

    sourceSets {
        main {
            manifest.srcFile 'src/isolated/AndroidManifest.xml'
            java.srcDirs = ['src/isolated/java']
            res.srcDirs = ['src/isolated/res']
            assets.srcDirs = ['src/isolated/assets']
        }
    }

    buildTypes {
        debug {
            debuggable true
            minifyEnabled false
        }
        release {
            minifyEnabled false
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation 'org.mozilla.geckoview:geckoview:129.0.+'
}
